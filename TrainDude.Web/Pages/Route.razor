@using TrainDude.Network.DTOs;
@using TrainDude.Network.Queries;

@page "/route/{Id}"

@inject IMediator mediator
@inject IJSRuntime js

<PageTitle>Odcinek</PageTitle>

<section class="section">
    <h1 class="title">Odcinek</h1>
    @if (this.route != null)
    {
        <p class="subtitle">@(this.route.AName) - @(this.route.BName)</p>
    }
</section>
<hr />
<section class="section">
    <div class="block">
        <div id="map" style="height:512px;"></div>
    </div>
</section>

@code {
    private IJSObjectReference scriptModule;
    private RouteDetailsDTO route;
    private string routeGeoJson;


    [Parameter]
    public int Id { get; set; }

    protected override async Task OnParametersSetAsync()
    {
        this.route = await this.mediator.Send(new GetRouteQuery(this.Id));
        this.routeGeoJson = await this.mediator.Send(new GetRouteGeoJsonQuery(this.Id));
        var antiradiusSeries = await this.mediator.Send(new GetRouteAntiradiusSeriesQuery(this.Id, 20));

        await base.OnParametersSetAsync();
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            this.scriptModule = await this.js.InvokeAsync<IJSObjectReference>("import", "./Pages/Route.razor.js");

            await this.scriptModule.InvokeVoidAsync("initMap", "map", route.ALocation!.Latitude, route.ALocation!.Longitude, 12);
            await this.scriptModule.InvokeVoidAsync("addGeoJson", this.routeGeoJson);
        }

        await base.OnAfterRenderAsync(firstRender);
    }
}

@using TrainDude.Application.Requests.GetRadiiQuery

@page "/radii"

@inject IMediator mediator

<PageTitle>Promienie</PageTitle>

<section class="section">
    <h1 class="title">Promienie</h1>
    <p class="subtitle">Lista minimalnych promieni łuku.</p>
</section>
<hr />
<section class="section">
    <div class="table-container">
        <table class="table">
            <thead>
                <tr>
                    <th>Prędkość maksymalna</th>
                    <th>Minimalny promień łuku</th>
                    <th>Maksymalny antypromień łuku</th>
                </tr>
            </thead>
            <tbody>
                @if (this.radii == null)
                {
                    <tr>
                        <td colspan="3"><em>Loading...</em></td>
                    </tr>
                }
                else
                {
                    @foreach (var radius in this.radii)
                    {
                        <tr>
                            <td>@($"{radius.Speed}") km/h</td>
                            <td>@($"{radius.Minimum}") km</td>
                            <td>@($"{radius.MaximumAntiradius:0.00}") 1/Mm</td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</section>

@code {
    private IEnumerable<GetRadiiQueryResultItem> radii;

    protected override async Task OnParametersSetAsync()
    {
        var result = await this.mediator.Send(new GetRadiiQuery());
        this.radii = result.Items.ToList();

        await base.OnParametersSetAsync();
    }
}
